# 🧠 Python Fundamentals

Este diretório concentra os **fundamentos da linguagem Python**, servindo como base para todos os outros módulos do repositório. Aqui estão os conceitos essenciais necessários para compreender, escrever e manter código Python de forma clara e eficiente.

---

## 🔢 Tipos de Dados

Python possui tipos de dados nativos que permitem representar informações de diferentes formas:

- **`int`** — números inteiros  
- **`float`** — números decimais  
- **`str`** — cadeias de caracteres (texto)  
- **`bool`** — valores lógicos (`True` / `False`)  
- **Coleções** — `list`, `tuple`, `set`, `dict`  

Esses tipos são a base para qualquer operação lógica ou computacional na linguagem.

---

## ➗ Operadores

Os operadores permitem realizar cálculos, comparações e tomadas de decisão:

- **Aritméticos:** `+`, `-`, `*`, `/`, `//`, `%`, `**`
- **Relacionais:** `==`, `!=`, `>`, `<`, `>=`, `<=`
- **Lógicos:** `and`, `or`, `not`
- **Identidade:** `is`, `is not`
- **Associação:** `in`, `not in`

---

## 🔁 Estruturas de Controle

Permitem alterar o fluxo de execução do programa com base em condições ou repetições:

### Condicionais
- `if`
- `elif`
- `else`

### Laços de repetição
- **`for`** — iteração sobre sequências  
- **`while`** — repetição baseada em condição lógica  

---

## 📦 Coleções de Dados

Estruturas utilizadas para armazenar e organizar múltiplos valores:

- **Listas (`list`)** — mutáveis e ordenadas  
- **Tuplas (`tuple`)** — imutáveis e ordenadas  
- **Conjuntos (`set`)** — não ordenados e sem duplicatas  
- **Dicionários (`dict`)** — pares chave/valor  

---

## 🧵 Strings

Strings são sequências de caracteres e suportam diversas operações:

- Fatiamento (*slicing*)
- Métodos nativos (`upper`, `lower`, `split`, `replace`)
- Interpolação com *f-strings*
- Manipulação e formatação de texto

---

## ⏰ Data e Hora

Manipulação de datas e horários utilizando a biblioteca padrão:

- `datetime`
- `date`
- `time`
- `timedelta`
- Formatação com `strftime` e `strptime`
- Controle de fuso horário

---

## 🧪 Exercícios e Desafios

A pasta de desafios contém exercícios práticos voltados para:

- Fixação de conceitos
- Desenvolvimento do raciocínio lógico
- Simulação de problemas reais

---

📌 **Observação:**  
Os conteúdos deste diretório formam a base necessária para avançar para **Programação Orientada a Objetos**, **Recursos Avançados da Linguagem** e **Projetos Práticos** presentes nos demais módulos do repositório.
