# 🚀 Python Advanced

Este diretório reúne **recursos avançados da linguagem Python**, explorando conceitos que elevam a qualidade, expressividade e poder do código. Os tópicos aqui abordados são amplamente utilizados em aplicações profissionais, bibliotecas, frameworks e arquiteturas modernas.

---

## 🎯 Funções Avançadas

- Funções como **objetos de primeira classe**.
- Possibilidade de:
  - Atribuir funções a variáveis
  - Passar funções como argumentos
  - Retornar funções de outras funções
- Uso extensivo em callbacks, decoradores e programação funcional.

---

## 🧠 Funções Internas (Inner Functions)

- Funções definidas dentro de outras funções.
- Utilizadas para:
  - Encapsular lógica
  - Criar closures
  - Controlar escopo

Esse recurso permite maior controle sobre o comportamento interno das funções.

---

## 🎨 Decoradores

- Funções que modificam o comportamento de outras funções.
- Executam código **antes e/ou depois** da função decorada.
- Uso de:
  - Decoradores simples
  - Decoradores com argumentos
  - Decoradores com retorno de valores
  - Açúcar sintático (`@decorator`)
- Aplicações comuns:
  - Logs
  - Validações
  - Autenticação
  - Medição de desempenho

---

## 🔁 Geradores

- Funções que produzem valores sob demanda usando `yield`.
- Permitem:
  - Economia de memória
  - Processamento eficiente de grandes volumes de dados
- Ideais para iteração preguiçosa (lazy evaluation).

---

## 🔄 Iteradores

- Objetos que implementam os métodos:
  - `__iter__()`
  - `__next__()`
- Base para estruturas de repetição em Python.
- Fundamentais para entender o funcionamento interno de loops como `for`.

---

## 🧪 Introspecção

- Capacidade de inspecionar objetos em tempo de execução.
- Uso de:
  - `dir()`
  - `type()`
  - `callable()`
  - `__dict__`
- Muito utilizada em frameworks, validações dinâmicas e metaprogramação.

---

📌 **Observação:**  
O domínio desses conceitos permite escrever código mais **flexível**, **performático** e **expressivo**, sendo um diferencial importante para quem deseja atuar com desenvolvimento avançado, automações, bibliotecas ou engenharia de software em Python.
