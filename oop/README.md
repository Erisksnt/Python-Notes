# 🧩 Programação Orientada a Objetos (OOP)

Este diretório é dedicado aos **conceitos de Programação Orientada a Objetos em Python**, um paradigma fundamental para o desenvolvimento de aplicações escaláveis, organizadas e de fácil manutenção. Os exemplos aqui apresentados demonstram como estruturar código de forma mais robusta e reutilizável.

---

## 🧱 Classes e Objetos

- **Classe** — modelo ou estrutura que define atributos e comportamentos.
- **Objeto** — instância concreta de uma classe.
- Definição de atributos de classe e de instância.
- Criação e manipulação de objetos.

Esses conceitos permitem representar entidades do mundo real dentro do código.

---

## 🔐 Encapsulamento

- Controle de acesso aos dados internos de uma classe.
- Uso de atributos públicos, protegidos e privados.
- Protege o estado interno do objeto e evita modificações indevidas.

O encapsulamento melhora a segurança e a previsibilidade do código.

---

## 🏗️ Construtores e Destrutores

- **Construtor (`__init__`)** — inicializa o objeto no momento da criação.
- **Destrutor (`__del__`)** — executado quando o objeto é removido da memória.
- Responsáveis pelo ciclo de vida do objeto.

---

## 🧬 Herança

- Permite que uma classe herde atributos e métodos de outra.
- **Herança simples** — uma classe base.
- **Herança múltipla** — múltiplas classes base.
- Reutilização e extensão de código.

---

## 🔄 Polimorfismo

- Capacidade de diferentes classes responderem ao mesmo método de formas distintas.
- Facilita a escrita de código genérico e flexível.
- Muito utilizado em herança e interfaces.

---

## 🧾 Métodos de Classe e Estáticos

- **Métodos de instância** — operam sobre o objeto.
- **Métodos de classe (`@classmethod`)** — operam sobre a classe.
- **Métodos estáticos (`@staticmethod`)** — não dependem de instância nem de classe.

Esses métodos ajudam a organizar responsabilidades dentro da classe.

---

## 🧠 Properties

- Uso do decorador `@property`.
- Permite acessar métodos como se fossem atributos.
- Facilita validações e encapsulamento sem alterar a interface pública.

---

## 🧱 Classes Abstratas

- Definem uma estrutura base para outras classes.
- Não podem ser instanciadas diretamente.
- Utilizam:
  - O módulo `abc`
  - O decorador `@abstractmethod`

Forçam que subclasses implementem métodos obrigatórios, garantindo consistência.

---

## 🧪 Desafios Práticos

- Exercícios aplicados para:
  - Fixação dos conceitos de OOP
  - Simulação de cenários reais
  - Desenvolvimento de soluções orientadas a objetos

---

📌 **Observação:**  
Os conceitos apresentados neste diretório são essenciais para compreender **arquiteturas de software**, **APIs**, **frameworks modernos** e **projetos de médio e grande porte** em Python.
